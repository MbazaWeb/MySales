/**
 * This project uses Supabase as its database.
 * See lib/supabase/ for the client, server, types, actions and hooks.
 *
 * This file is kept so the Cloudflare Worker entry (worker/index.ts) and
 * drizzle.config.ts compile without errors. The D1 binding is unused.
 */
export {};
